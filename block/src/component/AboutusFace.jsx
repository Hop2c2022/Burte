export const AboutUsFace = ({ width, height, src }) => {
    return (
      <img
        style={{ borderRadius: "50px" }}
        width={width}
        height={height}
        src={src}
      />
    );
  };